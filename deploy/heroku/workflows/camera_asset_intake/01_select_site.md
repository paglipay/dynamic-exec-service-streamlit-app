## Step 1: Select site

Pick the school this intake session is for. The list comes from the
same site directory the Google Earth tool uses.

Every step after this one is scoped to the site you pick here — camera
chart rows, scanned hardware, and assignments are all stored per site
(keyed by Location Code), so switching sites mid-session is safe; each
site's data stays separate.
